function openShop(){
    setTimeout(() =>{
        texta.textContent("Welcome to the shop,");
        texta.textContent("\nwhat would you like to buy?: ")
    },5000);
    texta.textContent(shopItem(weapon[x].name, weapon[].shopvalue));
    texta.textContent(shopItem(potion[x].name, potion[].shopvalue));
}

weapons.length
potion.length

function shopItems(x){
    for(var i = 0; i < 3; i++){
        let randomItem = Console.log(Math.floor(Math.random() * Math.random()));
        return(randomItem);
    }
}


Console.log(Math.floor(Math.random() * Math.random()));